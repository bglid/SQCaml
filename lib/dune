(include_subdirs unqualified)

(library
 (name SQCaml)
 (preprocess
  (pps ppx_deriving.show))
 (libraries unix)
 (instrumentation
  (backend bisect_ppx)))

(ocamllex lexer)

(menhir
 (modules parser))
